name: 🔒 CI Gate

on:
  push:
    branches: [ "main" ]

jobs:
  initial-check:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 💾 Check required files
        run: |
          # 检查必要的目录和文件是否存在
          if [ ! -f "tools/gen_dist.js" ]; then
            echo "❌ Missing tools/gen_dist.js"
            exit 1
          fi
          echo "✅ All required files exist."
