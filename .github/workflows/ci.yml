name: CI
on:
  push:
    branches: [ main ]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm init -y
      - name: Run contract verification
        run: echo "Contract verification placeholder"
      - name: Run unit tests
        run: echo "Unit tests placeholder"
      - name: Dist consistency check
        run: echo "Dist check placeholder"
